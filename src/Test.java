public class Test extends Task implements AutoChecked {
    private String[] arrAnswers; // Массив с ответами

    // Конструктор класса Task
    public Test() {
        super();
        this.arrAnswers = new String[]{"a", "b", "c"};
    }
}
